if (MSVC)
    add_subdirectory(windows/)
elseif(APPLE)
    add_subdirectory(macos/)
elseif(UNIX)
    add_subdirectory(linux/)
else()
    message(FATAL_ERROR "Unsupported compiler or OS")
endif()