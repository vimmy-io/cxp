#include <engine-ui-exec/exec.h>

#include <engine-build/engine-build.h>

int main() {
  exec::Exec MainProgramObject;

  MainProgramObject.DoWorldPeace();

  NGINE::Engine asdf;

  return asdf.get_frames();
}
